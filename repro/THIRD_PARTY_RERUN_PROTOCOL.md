# Third-Party Rerun Protocol

1. Clone the repository.
2. Run `bash repro/run_repro.sh`.
3. Inspect `repro/certificate_runtime.json`.
4. Verify that `all_pass == true`, the strict margin is positive, and the release gate reports `ok: true`.
