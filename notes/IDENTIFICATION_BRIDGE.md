# Identification Bridge

This note records the determining-class bridge for `unlikely intersections`. It fixes the endpoint representative, tracks `eps_coh`, and closes the final strict margin `M_UIE` in strict-coherence mode.
